#ifndef LAND_TYPE_H
#define LAND_TYPE_H

/*
this header is included in every class, so the definition of std includes will be here.
*/

#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include <math.h>


enum LandType
{
    none,
    obstacle,
    unit
};

#endif